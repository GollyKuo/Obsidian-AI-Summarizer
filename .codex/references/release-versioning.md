# Release Versioning Reference

版本節點至少同步：

1. `package.json` version
2. `manifest.json` version
3. `docs/dev_log.md` 條目

若 `manifest.json` / `package.json` 尚未建立，先記錄到 `docs/dev_log.md`，等 scaffold 建立後補齊版本一致性。
